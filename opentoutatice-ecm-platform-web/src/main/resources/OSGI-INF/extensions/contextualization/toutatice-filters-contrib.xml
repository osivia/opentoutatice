<?xml version="1.0"?>
<component name="fr.toutatice.ecm.platform.web.contextualization.filters">

	<require>org.nuxeo.ecm.platform.actions.ActionService</require>

	<extension target="org.nuxeo.ecm.platform.actions.ActionService"
		point="filters">

		<filter id="filter@TTC_TAB_ADMIN_PUBLICATION_SPACE">
			<rule grant="true">
				<condition>document.hasFacet('PortalConfigurable')</condition>
			</rule>
		</filter>

	</extension>

</component>